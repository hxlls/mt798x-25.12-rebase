# 硬件小包优先（MT798x）讨论笔记

> 记录于 immortalwrt-mt798x-rebase 源码分析。内容为"硬件小包优先"的实现方案与改动清单。

## 背景结论

- MT798x 的 L3（路由/NAT 转发）**硬件卸载**，由内置 PPE（Packet Processing Engine / FOE）完成，不占 CPU。
- 硬件卸载最小调度单元是 **FOE entry（一条流）**，不是单个包。PPE 命中流表后按 entry 内 QID 放队列，再经硬件 shaper/HQoS 调度。
- 因此"硬件小包优先"= 把**小包型流**识别出来，整条流映射到高优先级硬件队列（逐包分类做不到）。
- 本仓库实际设计走 **mtkhnat 路径**：`luci.turboacc` 中 `hasFLOWOFFLOADING: false // Explicitly disabled for mt798x`，配套 `hnat-detect` / `mtkhqos_util` 全部指向 `/sys/kernel/debug/hnat`。
- 硬件队列基础设施已完整（HQoS/PPPQ、16 个 QDMA 队列），缺的是"小包流 → 高优先级队列"的映射。

## 仓库里已有的两条硬件卸载路径

1. **PPE flow offload**：`CONFIG_NF_FLOW_TABLE=y`、`CONFIG_NET_MEDIATEK_SOC=y`
   - 卸载实现：`drivers/net/ethernet/mediatek/mtk_ppe*.c`（由 `999-ppe-*` 补丁扩展）
   - debugfs：`/sys/kernel/debug/mtk_ppe/qos_toggle`（0=关 1=HQoS 2=PPPQ 3=adaptive PPPQ）
2. **mtk_hnat 专有驱动**：`CONFIG_NET_MEDIATEK_HNAT`（默认 `is not set`，见 `target/linux/mediatek/filogic/config-6.12:338`）
   - 驱动：`drivers/net/ethernet/mediatek/mtk_hnat/`（源码在 feeds，补丁 `999-*mtkhnat-*`）
   - debugfs：`/sys/kernel/debug/hnat/`（`hnat_ppd_if` / `hnat_wan_if` / `hnat_lan_if` / `hnat_lan2_if` / `hook_toggle`）

## 已有的"小包优先"雏形

- `999-ppe-11-mtk_ppe-dispatch-short-packets-a-high-priority.patch`：
  - `mtk_flow_is_tcp_ack()` 用 conntrack 统计平均包长 ≤64B 判定 TCP ACK 流；
  - PPPQ 路径（`qos_toggle==2`）命中时 `queue += 6` 插到高优队列；
  - 限制：仅 TCP、仅 DSA 口（`dsa_port>=0`）、仅 ACK 类流。
- `999-ppe-04`：HQoS/PPPQ 模式 + debugfs 开关。
- `999-ppe-20`：adaptive PPPQ（`qos_toggle=3`），`mtk_shaper_update_refcnt` 动态控制 shaper 流总数（阈值 6000）。
- `mtk_flow_set_output_device`（`999-ppe-04`）已支持读 `ct->mark`（`MTK_QDMA_QUEUE_MASK`）映射硬件队列。

## 改动清单

### A. 配置层（已有，只需开启）
- `/etc/config/mtkhqos`（包 `mtkhqos_util`）：`option hqos 1`，调整 queue `weight/minrate/maxrate`。
- PPE 路径开 PPPQ：`echo 2 > /sys/kernel/debug/mtk_ppe/qos_toggle`（3=adaptive）。
- `package/network/config/firewall/files/firewall.config:6`：`option flow_offloading 1`（flow offload 路径）或确保 HNAT `hook_toggle=enabled`。
- dnsmasq connmark allowlist（`200-ubus_dns.patch` 已支持）：域名级小包流识别。

### B. 内核代码改动（核心，必须新增）
1. **ctmark 在 PPPQ 下生效**：`mtk_flow_set_output_device`（`999-ppe-04/11/20`）现在 PPPQ 分支 `queue=3+dsa_port` 直接覆盖 ctmark → 需改成"默认队列 + 高优流（ctmark/短包判定）覆盖为高队列"。
2. **短包流识别泛化**：`mtk_flow_is_tcp_ack`（`999-ppe-11`）从"仅 TCP ACK ≤64B"泛化为 UDP/游戏/DNS/VoIP 短包流，阈值可配。
3. **新增 debugfs 开关**：`mtk_ppe_internal_debugfs_init`（`999-ppe-04`）加 `/sys/kernel/debug/mtk_ppe/short_pkt_qos`。
4. **（HNAT 路径）ctmark→队列**：`hnat_nf_hook.c`（feeds 内）确认/新增按 skb/ct mark 选 QID 的 hook。

### C. 用户态打标规则（必配）
- fw4（`/etc/config/firewall` rule 或 `/etc/firewall.user`）加 mangle 规则，给小包流（游戏端口、DSCP、dnsmasq connmark 命中）打 `ct mark`，值对应高优先级队列号（`MTK_QDMA_QUEUE_MASK` 范围）。

### D. 验证
- `cat /sys/kernel/debug/mtk_ppe/qos_toggle`
- `hnat_stats`（`luci.turboacc` 已能读取）
- `qdma_txq*` 队列命中计数

## 关键取舍/局限
- 卸载后的流不经过 CPU，SQM/CAKE 等软件 QoS 对它无效（二者互斥）。
- 队列资源有限（PPPQ qid 3~14），误判会把大流抬进高优队列。
- 64B 阈值武断，需实测调整。

## 最小可跑通方案
A + C，依赖厂商已支持的 ctmark→队列逻辑（`999-ppe-04`，`qos_toggle==1` 时读 `ct->mark`）。B 是把逻辑做精细（PPPQ 下生效 + 短包自动识别）。

## 已实现（2026-08-19，待编译测试）

### 内核补丁 `target/linux/mediatek/patches-6.12/999-ppe-45-mtk_ppe-add-short-packet-qos.patch`
- `mtk_eth_soc.h`：新增 `short_pkt_qos`/`short_pkt_len` 字段与宏
  `MTK_QDMA_SHORT_PKT_QID_MIN=12`、`MTK_QDMA_SHORT_PKT_QID_DELTA=6`、`MTK_QDMA_SHORT_PKT_LEN_DEF=64`
- `mtk_ppe_offload.c`：
  - `mtk_flow_is_tcp_ack()` → `mtk_flow_is_short_pkt()`：TCP/UDP 通用、双向平均包长 ≤ `short_pkt_len`（默认 64），支持 ACK/DNS/VoIP/游戏保活
  - PPPQ 分支（qos_toggle 2/3）：`short_pkt_qos` 开启时 ctmark 高优队列（qid≥12）优先于默认队列；`short_pkt_qos>1` 时自动短包识别 `queue += 6`
- `mtk_ppe_debugfs.c`：新增 `/sys/kernel/debug/mtk_ppe/short_pkt_qos`（0 关 / 1 仅 ctmark / 2 ctmark+自动）、`short_pkt_len`（16-128 字节）；修复 `read_qos` 用 `pr_info` 导致 `cat qos_toggle` 为空的问题（改为 `seq_printf` 输出数字），供用户态判断是否需开启 QoS

### eqos 包 `package/mtk/applications/luci-app-eqos-mtk/`
- `root/etc/config/eqos`：新增 `short_pkt_priority`/`short_pkt_mode`/`short_pkt_len`/`short_pkt_qid`/`short_pkt_ports`/`short_pkt_dscp`
- `root/usr/sbin/eqos`：新增 `shortpkt` 命令 + `apply_short_pkt()`/`stop_short_pkt()`；写 PPE debugfs + nft ctmark 规则（端口/DSCP）；确保 PPE `qos_toggle` 非 0
- `root/etc/init.d/eqos`：`eqos enable` 后调用 `eqos shortpkt`
- `htdocs/luci-static/resources/view/eqos.js`：设置区新增"小包优先"开关及参数
- `po/templates/eqos.pot`、`po/zh_Hans/eqos.po`：翻译

### 测试注意
- 硬件自动短包识别走 flow offload 路径（PPE `short_pkt_qos`），需要 `qos_toggle`（PPE）非 0
- eqos 现有 HQoS/HNAT 路径不受影响；nft ctmark 规则对 flow offload 生效，HNAT 路径用 skb mark 不受影响
- 验证：`cat /sys/kernel/debug/mtk_ppe/short_pkt_qos`、`cat /sys/kernel/debug/mtk_ppe/short_pkt_len`

### 已知限制与待办
- **用户设备 Zyxel EX5700 (Telenor)**：WAN = gmac1 直连 2.5G PHY（非 DSA 口），LAN = MT7531 交换机（DSA）。mode 2 自动识别只在 DSA/PPPQ 路径生效 → WAN 方向（LAN→WAN 上行）不生效。
- **方案 3（已实现 2026-08-19）**：短包自动识别已扩展到 `qos_toggle==1`（HQoS）路径，摆脱 PPPQ 依赖，2.5G 直连口 / 无固定端口游戏也能自动识别。新增 `short_pkt_qid` debugfs 节点（1~62，默认 14）+ `mtk_flow_is_short_pkt` 最小样本数 20 包防误判。eqos 工具同步写 `short_pkt_qid`。不改变卸载本身（只影响 QID 选择），不会导致无法硬件加速。
- **无固定端口游戏**：方案 1 按主机 IP 打高优 mark（立即可用）；方案 2 终端 DSCP 标记；方案 3 自动短包识别（已实现，任何口生效）。

### HQoS 队列监控（已实现 2026-08-19）
- **内核补丁 `999-zzz-5500-mtk_ppe-add-qdma-queue-stats.patch`**：
  - `/sys/kernel/debug/mtk_ppe/qdma_queue_stats`：遍历 `eth->flow_table`，每流取 `mtk_foe_entry_get_queue()` qid + `mtk_foe_entry_get_stats()` 累计 bytes/packets，按 qid 聚合（硬件卸载路径）
  - `/sys/kernel/debug/mtk_ppe/qdma_tx_stats`：`mtk_tx_map` 按 `skb_get_queue_mapping` 累加 per-queue 软计数（软件/CPU 路径）
- **rpcd 后端 `package/mtk/applications/luci-app-eqos-mtk/root/usr/share/rpcd/ucode/luci.eqos`**：`getQueueStats()`（读 qdma_pppq 的 speed/refcnt + 两个 stats，两次采样差值得实时 Mbps/pps）、`getShortPkt()`（读 short_pkt_qos/len/qid/qos_toggle）；acl 已放行 `luci.eqos`
- **LuCI**：eqos.js 顶部新增"队列监控"区块（poll 轮询表格：队列/限速/流数/速率/包率）+ 中英文翻译
- 说明：硬件卸载流不经 QDMA tx ring，per-queue 真实吞吐来自 PPE MIB 聚合；QDMA 软计数只反映软件路径
- 验证：`cat /sys/kernel/debug/mtk_ppe/qdma_queue_stats`、`cat /sys/kernel/debug/mtk_ppe/qdma_tx_stats`

### HNAT 路径移植（2026-08-19，用户设备实际路径）
- **重要事实**：用户设备（EX5700 Telenor）固件开了 `CONFIG_NET_MEDIATEK_HNAT`，导致 `mtk_eth_soc.c:7519` 的 `#if !defined(CONFIG_NET_MEDIATEK_HNAT)` 把整个 flow offload（mtk_ppe）路径编译排除——**flow offload 路径的 5400/5500 补丁在 HNAT 固件下无效**，`/sys/kernel/debug/mtk_ppe` 不存在。
- **5400/5500 补丁已删除（2026-08-19）**：用户确认走 HNAT 路径，移除 flow offload 的两个补丁。
- **监控适配 HNAT（2026-08-19）**：`luci.eqos` 的 `getShortPkt` 优先读 `/sys/kernel/debug/hnat/`（short_pkt_toggle/qid/len/qos_toggle）；`getQueueStats` 读 `/sys/kernel/debug/mtketh/qdma_pppq`（speed/refcnt，HNAT 和 flow offload 都有），flow offload 的 FOE/MIB 计数节点存在时才算速率（`has_counters` 标志）；eqos.js 信息栏显示当前路径（hnat/ppe），无计数时速率列显示 `-`。
- **正确路径**：`mtk_hnat` 源码在 `target/linux/mediatek/files-6.12/drivers/net/ethernet/mediatek/mtk_hnat/`（OpenWrt files 机制拷入内核树，可持久修改）。
- **已实现**（HNAT 短包识别）：
  - `hnat.h`：extern `short_pkt_toggle`/`short_pkt_qid`/`short_pkt_len`
  - `hnat_debugfs.c`：定义变量（qid 默认 14、len 默认 64）+ `/sys/kernel/debug/hnat/` 下新增 `short_pkt_toggle`/`short_pkt_qid`/`short_pkt_len` 节点
  - `hnat_nf_hook.c`（`skb_to_hnat_info`，原 2844-2872）：`short_pkt_toggle` 开启时，TCP ACK（payload=0）或 UDP 小包（payload≤len）→ `qid = short_pkt_qid`，**任何口生效（含 2.5G 直连）**；原 PPPQ ACK 逻辑保留在 else 分支
  - eqos 工具：`apply_short_pkt` 同时写 HNAT 节点（HNAT 优先），`stop_short_pkt` 关闭
- **HNAT 用 skb->mark**（非 ct mark）：eqos 的 device 规则（按 IP 打 skb mark）在 HNAT 下本来就有效；HNAT 无"ctmark only"概念。
- **模式已简化（2026-08-19）**：HNAT 下 mode 1/2 行为相同（都是 per-packet 识别），已移除 `short_pkt_mode` 配置/UI，改为单一开关（`short_pkt_toggle` 非 0 即开）。eqos 直接写 1。
- **>1G 口无影响（2026-08-19 确认）**：HNAT 短包识别（`short_pkt_toggle`）**不检查端口速率**（不同于 flow offload 的 PPPQ ≤1G 限制），qid 是 HQoS 全局队列，2.5G 直连口照常生效。唯一注意：高优队列（qid14）的 `qdma_txq14` 带宽配置，若配了限速上限且小包流过多可能触及，但短包流本身流量小、且识别只发生在绑定前，实际无影响。
- **前提**：短包识别写入 entry 需 `qos_toggle`（hnat）非 0——eqos `start_hnat_qos` 已写 1。
- 验证：`cat /sys/kernel/debug/hnat/short_pkt_toggle`、`cat /sys/kernel/debug/hnat/short_pkt_qid`

## 会话二：修复/清理/自定义优先级规则/多页面（2026-08-20）

### 修复项
- **QoS 显示 `-`**：`hnat_debugfs.c` `hnat_qos_toggle_read` 用 `pr_info`（只打 dmesg）→ 改 `seq_printf(m, "%d\n", qos_toggle)`，`cat qos_toggle` 可读。
- **short_pkt 节点权限** `0444 → 0644`（可写，shell `-w` 检测通过）。
- **luci.eqos ucode 设备 ucode 兼容**：设备 ucode 不支持 `String()`/`Date.now()`/`Object.keys`！
  - `out[String(i)]` → `out[i]`（数字键）
  - `Date.now()` → `clock()`（`ck[0]*1000 + int(ck[1]/1e6)` 毫秒）
  - `Object.keys(x).length` → `length(x)`
  - 症状：`ubus call luci.eqos getQueueStats` 报 `Unknown error` → 队列监控不显示。
- **hdtdocs 编译缓存**：LuCI 包 htdocs 改后 build_dir 不刷新 → 需 `rm -f $(find build_dir staging_dir -name 'eqos.js')` + `make package/.../clean` + 重编。加 `PKG_VERSION:=1.0.1` 强制版本变化（ipk 名 + JS `?v=` 参数）。
- **启动缺陷**（2026-08-20 发现）：eqos 总开关 `enabled=0` 时 `start_service` 提前 return → **重启后小包优先/优先级规则不自动应用**（toggle 归零）。已修：enabled 判断只包 QoS 部分，短包/优先级始终应用。

### 移除/清理（HNAT-only）
- 删除 `short_pkt_ports`/`short_pkt_dscp`（ct mark 规则，HNAT 只读 skb mark，无效）+ 相关 UI/配置/翻译。
- ucode `getQueueStats` HNAT-only：删 `qdma_pppq`（v3 SoC 才有，v2 没有）、`mtk_ppe`（flow offload 不存在）；改读 `/sys/kernel/debug/hnat/qdma_txq0~63` 的 packet/bytes 计数（全队列监控）。
- eqos 工具删 `write_ppe`/`PPE_DIR`。
- **提示修正**：MTK 硬件**队列编号越大越优先**（厂商 ACK 逻辑 `qid += MAX_SWITCH_PORT_NUM`），但 EQoS 用 WRR 按 weight 调度，编号不严格决定优先级。

### 自定义优先级规则（新功能）
- UCI `config priority_rule`：`protocol`(tcp/udp/icmp/any)、`sport`/`dport`(端口/范围)、`dscp`、`direction`(up/down/both)、`priority`(1-10 档位)。
- 档位映射：上传 qid = `1 + (prio-1)*30/9`（→1..31），下载 qid = `32 + (prio-1)*30/9`（→32..62）；WRR weight = 档位。
- 机制：nft 打 skb mark（`iifname/oifname` 区分方向，sport+dport 双规则）→ HNAT `qid = skb->mark & 0x3f`（qos_toggle=1 时）。
- **优先级顺序**：自定义规则（mark）> 短包自动识别 > 默认。内核改：`if (short_pkt_toggle && !(skb->mark & MTK_QDMA_QUEUE_MASK))`（尊重 mark）。
- **短包 weight 提至 10**（最高档）：`apply_short_pkt` 里 `program_priority_queue "$qid" "$UP_SCH" 10`，避免被 10 级自定义压过。
- LuCI 冲突校验（保存红字）：规则间队列重复、与小包队列冲突、与设备规则队列占用 → validate 返回错误。
- eqos 工具 `priority` 命令；init.d `apply_priority_rules_service`。

### LuCI 多页面方案（2026-08-20 定稿）
- **放弃 form.Map `tabbed`**（每 section 一个 tab，无法合并 device/priority_rule 到自定义 tab）。
- 改为 **menu.d 多页面**（参考 luci-app-wechatpush）：3 个独立 view。
  - `view/eqos/qos.js`：HQoS 设置（enabled/download/upload + 设备规则 GridSection）
  - `view/eqos/priority.js`：包优先级设置（小包优先 + 优先级规则 GridSection）
  - `view/eqos/monitor.js`：队列监控（poll 轮询 + 活跃队列筛选 + 紧凑表格）
  - menu.d：`admin/services/eqos`（指向 qos）+ 3 子项（qos/priority/monitor），标题 HQoS Settings / Packet priority settings / Queue Monitor。
- 队列监控默认"仅显示活跃队列"，可切换全 64 队列。

### 翻译注意
- **po 双重转义 bug**：含 `"` 的 msgid（如 "Recognizes short flows...\"Short-flow length\"..."）被 python esc() 双重转义成 `\\\"` → gettext 解析含反斜杠 → 不匹配 JS → 翻译失效。已修：文件里应为 `\"`（0x5c 0x22）。

### 验证（2026-08-20 设备实测）
- 配置层：手动 `eqos shortpkt` → toggle=1、qos_toggle=1、qid=14、len=64 ✓
- **识别生效**：`dmesg | grep shortpkt` → `hnat shortpkt: TCP ACK -> qid 14`（识别日志已加入 hnat_nf_hook.c 短包分支，供确认）。
- UDP/DNS 本机查询不走 forward 识别路径（需局域网设备流量验证）。
- `qdma_txq%d` packet count **读后清零/不稳定**，不可用于增量验证。

### 环境
- 源码机 ai：192.168.1.226（`/home/ling/immortalwrt-mt798x-rebase`）
- 编译机 ubuntu：192.168.1.227（`/home/ling/imm2512`，ssh ling/Huang1122）
- 设备：192.168.1.1（root/Huang123!@#，Zyxel EX5700 Telenor，ImmortalWrt 25.12 r38309-2d0e93b125，内核 6.12.94，HNAT）
- 同步方式：tar 打包 → scp/paramiko 上传 → 解压替换；改内核需重编 target/linux。
